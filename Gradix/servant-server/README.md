# Gradix server
### This project uses Servant to build an API server. The server endpoints call some domain functions that are defined in a type class ModelAPI which defines crud operations on a generic database connection.
